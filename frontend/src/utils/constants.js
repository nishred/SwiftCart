export const tax = 200.99;


export const initialOptions = {
  "client-id": import.meta.env.VITE_CLIENT_ID,
  currency: "USD",
  intent: "capture",
};

export const IMAGE_URL = import.meta.env.VITE_IMAGE_URL;